# Sparky Website (local)

This is a static website for the Sparky chatbot with a small Node.js server that exposes `/api/chat` for demo purposes.

## Setup (basic)

1. Clone or copy the repo files.
2. Install server deps:
   ```bash
   cd server
   npm install
